<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.bran.branproxy.dao.base.ProxyPermissionBaseMapper">


    <insert id="insertProxyPermission"  useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `proxy_permission`
        (
        <trim suffixOverrides=",">
                    <if test="id!=null">
                        `id`,
                    </if>
                    <if test="permissionName!=null">
                        `permission_name`,
                    </if>
                    <if test="addUid!=null">
                        `add_uid`,
                    </if>
                    <if test="addTime!=null">
                        `add_time`,
                    </if>
                    <if test="updateUid!=null">
                        `update_uid`,
                    </if>
                    <if test="updateTime!=null">
                        `update_time`,
                    </if>
        </trim>
        )
        VALUES
        (
        <trim suffixOverrides=",">
                <if test="id!=null">
                    #{id},
                </if>
                <if test="permissionName!=null">
                    #{permissionName},
                </if>
                <if test="addUid!=null">
                    #{addUid},
                </if>
                <if test="addTime!=null">
                    #{addTime},
                </if>
                <if test="updateUid!=null">
                    #{updateUid},
                </if>
                <if test="updateTime!=null">
                    #{updateTime},
                </if>
        </trim>
        )
    </insert>




    <update id="updateProxyPermission">
        UPDATE `proxy_permission`
        SET
        <trim suffixOverrides=",">
            <if test="permissionName != null and permissionName!=''">
                `permission_name` = #{permissionName},
            </if>
            <if test="addUid != null and addUid!=''">
                `add_uid` = #{addUid},
            </if>
            <if test="addTime != null">
                `add_time` = #{addTime},
            </if>
            <if test="updateUid != null and updateUid!=''">
                `update_uid` = #{updateUid},
            </if>
            <if test="updateTime != null">
                `update_time` = #{updateTime},
            </if>
        </trim>
        WHERE
        <trim suffixOverrides="and">
                `id` = #{id} and
        </trim>
    </update>

    <update id="update">
        UPDATE `proxy_permission`
        SET
        <trim suffixOverrides=",">
        <if test="set.permissionName != null and set.permissionName!=''">
            `permission_name` = #{set.permissionName},
        </if>
        <if test="set.addUid != null and set.addUid!=''">
            `add_uid` = #{set.addUid},
        </if>
        <if test="set.addTime != null">
            `add_time` = #{set.addTime},
        </if>
        <if test="set.updateUid != null and set.updateUid!=''">
            `update_uid` = #{set.updateUid},
        </if>
        <if test="set.updateTime != null">
            `update_time` = #{set.updateTime},
        </if>
        </trim>
        <trim prefix="where" suffixOverrides="and | or">
                <if test="where.idList != null">
                    `id` in
                    <foreach collection="where.idList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.idSt !=null">
                `id` >= #{where.idSt} and
            </if>
            <if test="where.idEd!=null">
                `id` &lt;= #{where.idEd} and
            </if>
                <if test="where.permissionNameList != null">
                    `permission_name` in
                    <foreach collection="where.permissionNameList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyPermissionName!=null and where.fuzzyPermissionName.size()>0">
                (
                <foreach collection="where.fuzzyPermissionName"  separator="or" item="item">
                    `Permission_name` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyPermissionName!=null and where.rightFuzzyPermissionName.size()>0">
                (
                <foreach collection="where.rightFuzzyPermissionName"  separator="or" item="item">
                    `Permission_name` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.addUidList != null">
                    `add_uid` in
                    <foreach collection="where.addUidList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyAddUid!=null and where.fuzzyAddUid.size()>0">
                (
                <foreach collection="where.fuzzyAddUid"  separator="or" item="item">
                    `Add_uid` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyAddUid!=null and where.rightFuzzyAddUid.size()>0">
                (
                <foreach collection="where.rightFuzzyAddUid"  separator="or" item="item">
                    `Add_uid` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.addTimeList != null">
                    `add_time` in
                    <foreach collection="where.addTimeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.addTimeSt !=null">
                `add_time` >= #{where.addTimeSt} and
            </if>
            <if test="where.addTimeEd!=null">
                `add_time` &lt;= #{where.addTimeEd} and
            </if>
                <if test="where.updateUidList != null">
                    `update_uid` in
                    <foreach collection="where.updateUidList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test ="where.fuzzyUpdateUid!=null and where.fuzzyUpdateUid.size()>0">
                (
                <foreach collection="where.fuzzyUpdateUid"  separator="or" item="item">
                    `Update_uid` like concat('%',#{item},'%')
                </foreach>
                ) and
            </if>
            <if test ="where.rightFuzzyUpdateUid!=null and where.rightFuzzyUpdateUid.size()>0">
                (
                <foreach collection="where.rightFuzzyUpdateUid"  separator="or" item="item">
                    `Update_uid` like concat(#{item},'%')
                </foreach>
                ) and
            </if>
                <if test="where.updateTimeList != null">
                    `update_time` in
                    <foreach collection="where.updateTimeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
            <if test="where.updateTimeSt !=null">
                `update_time` >= #{where.updateTimeSt} and
            </if>
            <if test="where.updateTimeEd!=null">
                `update_time` &lt;= #{where.updateTimeEd} and
            </if>
        </trim>
    </update>


    <resultMap type="org.bran.branproxy.model.ProxyPermission" id="ProxyPermissionMap">
        <result property="id" column="id"/>
        <result property="permissionName" column="permission_name"/>
        <result property="addUid" column="add_uid"/>
        <result property="addTime" column="add_time"/>
        <result property="updateUid" column="update_uid"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>


    <select id="queryProxyPermission" resultMap="ProxyPermissionMap">
        select
        <include refid="baseResult"></include>
        from  `proxy_permission`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="id != null">
                `id` = #{id} and
            </if>
            <if test="permissionName != null and permissionName!=''">
                `permission_name` = #{permissionName} and
            </if>
            <if test="addUid != null and addUid!=''">
                `add_uid` = #{addUid} and
            </if>
            <if test="addTime != null">
                `add_time` = #{addTime} and
            </if>
            <if test="updateUid != null and updateUid!=''">
                `update_uid` = #{updateUid} and
            </if>
            <if test="updateTime != null">
                `update_time` = #{updateTime} and
            </if>
            <if test = "(_parameter instanceof org.bran.branproxy.model.ProxyPermission$QueryBuilder) == true">
                <if test="idList != null">
                    `id` in
                    <foreach collection="idList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="idSt !=null">
                    `id` >= #{idSt} and
                </if>
                <if test="idEd!=null">
                    `id` &lt;= #{idEd} and
                </if>
                <if test="permissionNameList != null">
                    `permission_name` in
                    <foreach collection="permissionNameList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyPermissionName!=null and fuzzyPermissionName.size()>0">
                    (
                    <foreach collection="fuzzyPermissionName"  separator="or" item="item">
                        `Permission_name` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyPermissionName!=null and rightFuzzyPermissionName.size()>0">
                    (
                    <foreach collection="rightFuzzyPermissionName"  separator="or" item="item">
                        `Permission_name` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="addUidList != null">
                    `add_uid` in
                    <foreach collection="addUidList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyAddUid!=null and fuzzyAddUid.size()>0">
                    (
                    <foreach collection="fuzzyAddUid"  separator="or" item="item">
                        `Add_uid` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyAddUid!=null and rightFuzzyAddUid.size()>0">
                    (
                    <foreach collection="rightFuzzyAddUid"  separator="or" item="item">
                        `Add_uid` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="addTimeList != null">
                    `add_time` in
                    <foreach collection="addTimeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="addTimeSt !=null">
                    `add_time` >= #{addTimeSt} and
                </if>
                <if test="addTimeEd!=null">
                    `add_time` &lt;= #{addTimeEd} and
                </if>
                <if test="updateUidList != null">
                    `update_uid` in
                    <foreach collection="updateUidList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test ="fuzzyUpdateUid!=null and fuzzyUpdateUid.size()>0">
                    (
                    <foreach collection="fuzzyUpdateUid"  separator="or" item="item">
                        `Update_uid` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyUpdateUid!=null and rightFuzzyUpdateUid.size()>0">
                    (
                    <foreach collection="rightFuzzyUpdateUid"  separator="or" item="item">
                        `Update_uid` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test="updateTimeList != null">
                    `update_time` in
                    <foreach collection="updateTimeList" close=")" open="(" separator="," item="item">
                        #{item}
                    </foreach> and
                </if>
                <if test="updateTimeSt !=null">
                    `update_time` >= #{updateTimeSt} and
                </if>
                <if test="updateTimeEd!=null">
                    `update_time` &lt;= #{updateTimeEd} and
                </if>
            </if>
        </trim>
    </select>

    <select id="queryProxyPermissionLimit1" resultMap="ProxyPermissionMap">
        select
        <include refid="baseResult"></include>
        from  `proxy_permission`
        <trim prefix="where" suffixOverrides="and | or">
            <if test="id != null">
                `id` = #{id} and
            </if>
            <if test="permissionName != null and permissionName!=''">
                `permission_name` = #{permissionName} and
            </if>
            <if test="addUid != null and addUid!=''">
                `add_uid` = #{addUid} and
            </if>
            <if test="addTime != null">
                `add_time` = #{addTime} and
            </if>
            <if test="updateUid != null and updateUid!=''">
                `update_uid` = #{updateUid} and
            </if>
            <if test="updateTime != null">
                `update_time` = #{updateTime} and
            </if>
            <if test = "(_parameter instanceof org.bran.branproxy.model.ProxyPermission$QueryBuilder) == true">
                    <if test="idList != null">
                        `id` in
                        <foreach collection="idList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="idSt !=null">
                    `id` >= #{idSt} and
                </if>
                <if test="idEd!=null">
                    `id` &lt;= #{idEd} and
                </if>
                    <if test="permissionNameList != null">
                        `permission_name` in
                        <foreach collection="permissionNameList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyPermissionName!=null and fuzzyPermissionName.size()>0">
                    (
                    <foreach collection="fuzzyPermissionName"  separator="or" item="item">
                        `Permission_name` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyPermissionName!=null and rightFuzzyPermissionName.size()>0">
                    (
                    <foreach collection="rightFuzzyPermissionName"  separator="or" item="item">
                        `Permission_name` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="addUidList != null">
                        `add_uid` in
                        <foreach collection="addUidList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyAddUid!=null and fuzzyAddUid.size()>0">
                    (
                    <foreach collection="fuzzyAddUid"  separator="or" item="item">
                        `Add_uid` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyAddUid!=null and rightFuzzyAddUid.size()>0">
                    (
                    <foreach collection="rightFuzzyAddUid"  separator="or" item="item">
                        `Add_uid` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="addTimeList != null">
                        `add_time` in
                        <foreach collection="addTimeList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="addTimeSt !=null">
                    `add_time` >= #{addTimeSt} and
                </if>
                <if test="addTimeEd!=null">
                    `add_time` &lt;= #{addTimeEd} and
                </if>
                    <if test="updateUidList != null">
                        `update_uid` in
                        <foreach collection="updateUidList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test ="fuzzyUpdateUid!=null and fuzzyUpdateUid.size()>0">
                    (
                    <foreach collection="fuzzyUpdateUid"  separator="or" item="item">
                        `Update_uid` like concat('%',#{item},'%')
                    </foreach>
                    ) and
                </if>
                <if test ="rightFuzzyUpdateUid!=null and rightFuzzyUpdateUid.size()>0">
                    (
                    <foreach collection="rightFuzzyUpdateUid"  separator="or" item="item">
                        `Update_uid` like concat(#{item},'%')
                    </foreach>
                    ) and
                </if>
                    <if test="updateTimeList != null">
                        `update_time` in
                        <foreach collection="updateTimeList" close=")" open="(" separator="," item="item">
                            #{item}
                        </foreach> and
                    </if>
                <if test="updateTimeSt !=null">
                    `update_time` >= #{updateTimeSt} and
                </if>
                <if test="updateTimeEd!=null">
                    `update_time` &lt;= #{updateTimeEd} and
                </if>
            </if>
        </trim>
        limit 1
    </select>

    <sql id="allResult">
        <trim suffixOverrides=",">
        `id`,
        `permission_name`,
        `add_uid`,
        `add_time`,
        `update_uid`,
        `update_time`,
        </trim>
    </sql>



    <sql id="baseResult">
        <trim suffixOverrides=",">
            <if test = "(_parameter instanceof org.bran.branproxy.model.ProxyPermission$QueryBuilder) == true">

                <if test="fetchFields==null">
                    <include refid="allResult"></include>
                </if>
                <if test="fetchFields!=null">
                    <if test="fetchFields.AllFields !=null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields ==null and fetchFields.fetchFields==null and fetchFields.excludeFields==null and fetchFields.otherFields==null">
                        <include refid="allResult"></include>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.fetchFields!=null">
                    <if test="fetchFields.fetchFields.id==true">
                        `id`,
                    </if>
                    <if test="fetchFields.fetchFields.permissionName==true">
                        `permission_name`,
                    </if>
                    <if test="fetchFields.fetchFields.addUid==true">
                        `add_uid`,
                    </if>
                    <if test="fetchFields.fetchFields.addTime==true">
                        `add_time`,
                    </if>
                    <if test="fetchFields.fetchFields.updateUid==true">
                        `update_uid`,
                    </if>
                    <if test="fetchFields.fetchFields.updateTime==true">
                        `update_time`,
                    </if>
                    </if>
                    <if test="fetchFields.AllFields==null and fetchFields.excludeFields!=null">
                    <if test="fetchFields.excludeFields.id==null">
                        `id`,
                    </if>
                    <if test="fetchFields.excludeFields.permissionName==null">
                        `permission_name`,
                    </if>
                    <if test="fetchFields.excludeFields.addUid==null">
                        `add_uid`,
                    </if>
                    <if test="fetchFields.excludeFields.addTime==null">
                        `add_time`,
                    </if>
                    <if test="fetchFields.excludeFields.updateUid==null">
                        `update_uid`,
                    </if>
                    <if test="fetchFields.excludeFields.updateTime==null">
                        `update_time`,
                    </if>
                    </if>
                    <if test="fetchFields.otherFields!=null and fetchFields.otherFields.size>0">
                        <foreach collection="fetchFields.otherFields" index="index" item="item" separator=",">
                        `#{item}`
                        </foreach>
                    </if>
                </if>
            </if>
            <if test="(_parameter instanceof org.bran.branproxy.model.ProxyPermission$QueryBuilder) == false" >
                <include refid="allResult"></include>
            </if>

        </trim>
    </sql>


</mapper>